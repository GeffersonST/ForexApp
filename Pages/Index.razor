@page "/"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Redirecionar para a página de login ao inicializar a página inicial
        NavigationManager.NavigateTo("/login");
    }
}
